<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.office.house.comment.ICommentDaoMapper">

    <insert id="insertNewReply" parameterType="com.office.house.comment.CommentDto">
        INSERT INTO
            tbl_comment (u_id, u_img, b_no, c_comment, c_reg_date, c_mod_date)
        VALUES
            (#{u_id}, #{u_img}, #{b_no}, #{c_comment}, now(), now())
    </insert>

    <select id="getMaxCno" resultType="Integer">
        SELECT
            max(c_no)
        FROM
            tbl_comment
    </select>

    <update id="updateTargetCno" parameterType="Integer">
        UPDATE
            tbl_comment
        SET
            c_target_c_no = #{c_no}
        WHERE
            c_no = #{c_no}
    </update>

    <select id="selectCommentList" parameterType="Integer" resultType="hashmap" >
        SELECT
            *
        FROM
            tbl_comment c
                JOIN tbl_board b
                    on
                        c.b_no = b.b_no
        WHERE
            c.b_no = #{b_no}
        ORDER BY
            c.c_target_c_no desc,
            c.c_reg_date asc;
    </select>

    <insert id="insertRereply" parameterType="com.office.house.comment.CommentDto">
        INSERT INTO
            tbl_comment (u_id, u_img, b_no, c_comment, c_target_u_id, c_target_c_no, c_reg_date, c_mod_date)
        VALUES
            (#{u_id}, #{u_img}, #{b_no}, #{c_comment}, #{c_target_u_id}, #{c_target_c_no}, now(), now())
    </insert>

    <delete id="deleteComment" parameterType="hashmap">
        DELETE FROM
                   tbl_comment
               WHERE
                   c_no = #{c_no}
    </delete>

    <update id="updateComment" parameterType="hashmap">
        UPDATE
            tbl_comment
        SET
            c_comment = #{commentDto.c_comment}, c_mod_date = now()
        WHERE
            c_no = #{c_no}
    </update>

    <update id="updateUserImage" parameterType="com.office.house.user.UserDto">
        UPDATE
            tbl_comment
        SET
            u_img = #{u_img}, c_mod_date = now()
        WHERE
            u_id = #{u_id}
    </update>

</mapper>